
# Integração conceptual com UI

A UI mostra o botão principal e um painel de estado. A UI não decide a ordem do pipeline nem a lógica de readiness; apenas chama o orchestrator e mostra o retorno. O payload de saída inclui estado do sistema, estado da Data/API, módulos corridos/ignorados e resumo final da corrida.
