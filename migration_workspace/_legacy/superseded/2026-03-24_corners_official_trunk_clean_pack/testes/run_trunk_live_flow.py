from motor.data_handoff.trunk_provider import load_fixtures_from_trunk, load_fixture_statistics_from_trunk

# Exemplo de fluxo real esperado:
# fixtures = load_fixtures_from_trunk(140, 2024)
# fixture_id = fixtures[0]['fixture']['id']
# stats = load_fixture_statistics_from_trunk(fixture_id, 140, 2024)
# depois adaptar -> motor -> contract output
