export const store = { route: 'home', system: null, pipeline: null };
export function setRoute(route){ store.route = route; }
export function setSystem(data){ store.system = data; }
export function setPipeline(data){ store.pipeline = data; }
