# Arquitetura UI v14

UI v14 = First Real Read Pack.

- `real_read_protected` como source mode explícito
- import manual de snapshot JSON
- validação mínima de shape
- fallback limpo para `orchestrator_mock`
