
# Nota curta de integração

Este pack mantém a bridge protegida em staging.

- não finge live total
- não consome diretamente o Orchestrator real
- usa leitura controlada e fallback disciplinado
- prepara entrada limpa em `migration_workspace/` e evolução futura para fonte real protegida
