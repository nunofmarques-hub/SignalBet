# Nota curta — fecho residual `league_id`

A v12 entra em fecho residual curto para resolver a derivação do campo `league_id` no bundle normalizado. O adapter/runtime já saiu do erro bruto e passou a falha auditável; falta apenas fechar este campo contratual para voltar a pôr a v12 a passar isoladamente e desbloquear a próxima tentativa disciplinada de shadow run ponta a ponta.
