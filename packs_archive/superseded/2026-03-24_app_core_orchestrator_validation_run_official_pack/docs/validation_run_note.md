
# Validation run

Este pack corre um `validation_run` real sobre uma estrutura local mínima do `Data_API_Official_Trunk_v1` incluída em `examples/demo_data_api/`. Quando existir um tronco oficial real no projeto, basta ajustar `project_root` no `sample_input.json` ou fornecer um caminho absoluto.
